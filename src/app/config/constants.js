const PROJECT_NAME = (process.env.PROJECT_NAME || 'App').replace(' ', '');

export const PRODUCTION = 'production';
export const DEVELOPMENT = 'development';
export const EN = 'en';
export const REDUX_NS = `@${PROJECT_NAME}`;
